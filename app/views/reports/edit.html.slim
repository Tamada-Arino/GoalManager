h3.is-size-4 日報編集
= render 'shared/errors', obj: @report
- small_goals = @goal.small_goals.order(:created_at).pluck(:title, :id)
div data-controller='slider' data-goal-color="#{@goal.color}"
  = render('form', models: [@goal, @report], goal: @goal, small_goals:)
